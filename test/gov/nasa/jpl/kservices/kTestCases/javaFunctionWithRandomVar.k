import gov.nasa.jpl.ae.event.DistributionFunctionCall
import gov.nasa.jpl.ae.util.distributions.*

var x : Distribution = Math.ceil(y)
var y : Exponential = Exponential(2)

var z : Real = P(x<3)
var zz : Real = P(x<=3)
